# This file ensures the projects/ directory exists in the repository
# Individual project containers are ignored by .gitignore
# This directory is used for storing project-specific containers and configurations 