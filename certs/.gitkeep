# This file ensures the certs/ directory exists in the repository
# SSL certificates and private keys are ignored by .gitignore for security
# This directory is used for storing domain-specific SSL certificates 