# This file ensures the proxy/certs/ directory exists in the repository
# SSL certificates and private keys are ignored by .gitignore for security
# This directory is used by the nginx proxy for domain-specific SSL certificates 